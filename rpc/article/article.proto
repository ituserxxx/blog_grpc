syntax = "proto3";

package article;
option go_package="./article";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Article {
  rpc Ping(Request) returns(Response);
}
